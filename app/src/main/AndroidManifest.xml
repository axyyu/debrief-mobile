<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kaiamelung.debrief">

    <application
        android:allowBackup="true"
        android:icon="@drawable/debrief2"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".FeedActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TagActivity"></activity>
        <activity android:name=".ChooseTagActivity"></activity>
        <activity android:name=".ArticleActivity"></activity>
        <activity android:name=".NotifActivity"></activity>
        <receiver android:name=".Notification"></receiver>
    </application>

</manifest>